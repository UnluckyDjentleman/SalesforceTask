/// <reference types="node" />
import { TestResult } from '../tests';
import { Readable, ReadableOptions } from 'node:stream';
export declare class HumanFormatTransform extends Readable {
    private readonly testResult;
    private readonly detailedCoverage;
    constructor(testResult: TestResult, detailedCoverage: boolean, options?: ReadableOptions);
    _read(): void;
    format(): void;
    private formatSummary;
    private formatTestResults;
    private formatDetailedCov;
    private formatCodeCov;
    private formatUncoveredLines;
}
