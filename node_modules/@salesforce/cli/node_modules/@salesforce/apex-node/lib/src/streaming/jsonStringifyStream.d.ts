/// <reference types="node" />
import { Readable, ReadableOptions } from 'stream';
interface JSONStringifyStreamOptions extends ReadableOptions {
    object: unknown;
}
export declare class JSONStringifyStream extends Readable {
    private sent;
    private lastYielded;
    private readonly object;
    constructor(options: JSONStringifyStreamOptions);
    private stringify;
    private yieldWithTracking;
    private handleObject;
    private handleArray;
    _read(): void;
    static from(json: unknown): JSONStringifyStream;
}
export {};
