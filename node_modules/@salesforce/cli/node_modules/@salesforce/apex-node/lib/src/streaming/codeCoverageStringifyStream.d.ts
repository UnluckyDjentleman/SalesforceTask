/// <reference types="node" />
import { Transform, TransformCallback } from 'stream';
import { PerClassCoverage } from '../tests';
import { TransformOptions } from 'node:stream';
export declare class CodeCoverageStringifyStream extends Transform {
    private outerPushed;
    private pushedInner;
    constructor(options?: TransformOptions);
    _transform(chunk: PerClassCoverage[], encoding: string, callback: TransformCallback): void;
    _flush(callback: TransformCallback): void;
    private transformCoverage;
}
